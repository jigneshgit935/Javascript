'use strict'; //treat all js a newer version

// alert(3+3) we are using nodejs, not browser

// code readability should be high
console.log(3 + 3);
console.log('Jignesh');

let name = 'jignesh'; //string
let age = 23; //number
let logged = true; //boolean
let state = null; //standlone value

// datatype
// number
// bigint
// string
// boolean true/false
// null =>standlone value ::::representation of empty value
// undefined==>when value is not assigned
// symbol => unique
// end---primitive datatypes

// object
console.log(typeof 2);
console.log(typeof 'jignesh');
console.log(typeof null); //---its an object (it is a type)
console.log(typeof undefined);
